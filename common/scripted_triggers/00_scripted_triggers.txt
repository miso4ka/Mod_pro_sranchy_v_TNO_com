### Civil Wars

german_civil_war = {
	OR = {
		SGR = {
			has_war_with = HGR
		}
		SGR = {
			has_war_with = BGR
		}
		SGR = {
			has_war_with = GER
		}
		SGR = {
			has_war_with = GGR
		}
		BGR = {
			has_war_with = HGR
		}
		BGR = {
			has_war_with = GER
		}
		BGR = {
			has_war_with = GGR
		}
		GGR = {
			has_war_with = GER
		}
		GGR = {
			has_war_with = HGR
		}
	}
}